# Server-side only. No token or signing secret is exposed to browser JavaScript.
BACKEND_URL=http://127.0.0.1:8000
# Set true behind HTTPS. false permits a local HTTP production-build smoke test.
SESSION_COOKIE_SECURE=false
